<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard - KEPL</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
  <style>
    :root {
      --primary-color: #5F9EA0;
      --secondary-color: #f7f9ff;
      --jobs-color: #66cacd;
      --brands-color: #61b9bc;
      --active-color: #4f8f91;
      --inactive-color: #38777b;
    }
    body {
      background-color: #f7f9ff;
      font-family: 'Poppins', sans-serif;
    }
    .sidebar {
      position: fixed;
      top: 0;
      left: 0;
      height: 100%;
      width: 250px;
      background-color: var(--primary-color);
      color: white;
      padding-top: 20px;
      z-index: 1;
    }
    .sidebar-header {
      padding: 0 1rem 1rem;
      border-bottom: 1px solid rgba(255,255,255,0.1);
      text-align: center;
    }
    .sidebar-header img {
      height: 40px;
      margin-bottom: 10px;
    }
    .nav-link {
      color: rgba(255,255,255,0.8);
      border-radius: 5px;
      margin: 0.2rem 1rem;
    }
    .nav-link:hover, .nav-link.active {
      background-color: rgba(255,255,255,0.1);
      color: white;
    }
    .nav-link i {
      margin-right: 10px;
    }
    .content {
      margin-left: 250px;
      padding: 20px;
    }
    .page-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
      padding-bottom: 10px;
      border-bottom: 1px solid #eee;
    }
    .btn-primary {
      background-color: var(--primary-color);
      border-color: var(--primary-color);
    }
    .department-badge {
      background-color: var(--primary-color);
      color: white;
      font-size: 0.8rem;
      padding: 0.25rem 0.5rem;
      border-radius: 4px;
    }
    .status-badge {
      font-size: 0.8rem;
      padding: 0.25rem 0.5rem;
      border-radius: 4px;
    }
    .active-badge {
      background-color: #d1e7dd;
      color: #0f5132;
    }
    .inactive-badge {
      background-color: #f8d7da;
      color: #842029;
    }
    .job-title, .brand-title {
      font-weight: 600;
      color: var(--primary-color);
    }
    .action-buttons .btn {
      padding: 0.25rem 0.5rem;
      font-size: 0.8rem;
    }
    .user-dropdown {
      margin-right: 20px;
    }
    .nav-tabs .nav-link {
      color: var(--primary-color);
    }
    .nav-tabs .nav-link.active {
      color: white;
      background-color: var(--primary-color);
      border-color: var(--primary-color);
    }
    .card-type-badge {
      background-color: #6c757d;
      color: white;
      font-size: 0.8rem;
      padding: 0.25rem 0.5rem;
      border-radius: 4px;
    }
    .info-card-badge {
      background-color: #17a2b8;
    }
    .image-card-badge {
      background-color: #6f42c1;
    }
    /* Stat Cards */
    .stat-card {
      border-radius: 10px;
      color: white;
      padding: 20px;
      margin-bottom: 20px;
      box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.15);
      transition: transform 0.3s;
    }
    .stat-card:hover {
      transform: translateY(-5px);
    }
    .stat-card.jobs {
      background-color: var(--jobs-color);
    }
    .stat-card.brands {
      background-color: var(--brands-color);
    }
    .stat-card.active {
      background-color: var(--active-color);
    }
    .stat-card.inactive {
      background-color: var(--inactive-color);
    }
    .stat-icon {
      font-size: 2rem;
      opacity: 0.7;
    }
    .stat-value {
      font-size: 1.5rem;
      font-weight: 700;
    }
    .stat-label {
      font-size: 0.9rem;
      text-transform: uppercase;
      letter-spacing: 0.1em;
    }
    .recent-activity {
      background: white;
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.1);
      margin-bottom: 20px;
    }
    .recent-activity h5 {
      color: var(--primary-color);
      margin-bottom: 15px;
      padding-bottom: 10px;
      border-bottom: 1px solid #eee;
    }
    .activity-item {
      padding: 10px 0;
      border-bottom: 1px solid #f8f9fa;
    }
    .activity-item:last-child {
      border-bottom: none;
    }
    .activity-date {
      font-size: 0.8rem;
      color: #6c757d;
    }
    @media (max-width: 992px) {
      .sidebar {
        width: 100%;
        height: auto;
        position: relative;
      }
      .content {
        margin-left: 0;
      }
    }
  </style>
</head>
<body>
  <div class="sidebar">
    <div class="sidebar-header">
      <img src="http://localhost:3000/assets/images/logokh.svg" alt="KEPL Logo">
      <h5>Admin Dashboard</h5>
    </div>
    <ul class="nav flex-column mt-3">
      <li class="nav-item">
        <a class="nav-link active" href="/admin/dashboard">
          <i class="bi bi-speedometer2"></i> Dashboard
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/admin/dashboard?tab=jobs">
          <i class="bi bi-briefcase"></i> Jobs Management
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/admin/dashboard?tab=brands">
          <i class="bi bi-tags"></i> Brands Management
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/admin/logout">
          <i class="bi bi-box-arrow-right"></i> Logout
        </a>
      </li>
    </ul>
  </div>

  <div class="content">
    <div class="page-header">
      <h3>Dashboard Overview</h3>
      <div class="user-dropdown">
        <span class="me-2">Welcome, Admin</span>
        <i class="bi bi-person-circle"></i>
      </div>
    </div>

    <% if (typeof error !== 'undefined') { %>
      <div class="alert alert-danger" role="alert">
        <%= error %>
      </div>
    <% } %>

    <!-- Stats Cards Row -->
    <div class="row">
      <div class="col-md-6 col-lg-3">
        <div class="stat-card jobs">
          <div class="d-flex justify-content-between align-items-center">
            <div>
              <div class="stat-value"><%= stats.totalJobs %></div>
              <div class="stat-label">Total Jobs</div>
            </div>
            <i class="bi bi-briefcase stat-icon"></i>
          </div>
        </div>
      </div>
      <div class="col-md-6 col-lg-3">
        <div class="stat-card brands">
          <div class="d-flex justify-content-between align-items-center">
            <div>
              <div class="stat-value"><%= stats.totalBrands %></div>
              <div class="stat-label">Total Brands</div>
            </div>
            <i class="bi bi-tags stat-icon"></i>
          </div>
        </div>
      </div>
      <div class="col-md-6 col-lg-3">
        <div class="stat-card active">
          <div class="d-flex justify-content-between align-items-center">
            <div>
              <div class="stat-value"><%= stats.activeJobs %></div>
              <div class="stat-label">Active Jobs</div>
            </div>
            <i class="bi bi-check-circle stat-icon"></i>
          </div>
        </div>
      </div>
      <div class="col-md-6 col-lg-3">
        <div class="stat-card inactive">
          <div class="d-flex justify-content-between align-items-center">
            <div>
              <div class="stat-value"><%= stats.inactiveBrands %></div>
              <div class="stat-label">Inactive Brands</div>
            </div>
            <i class="bi bi-x-circle stat-icon"></i>
          </div>
        </div>
      </div>
    </div>

    <!-- Recent Activity Row -->
    <div class="row">
      <div class="col-lg-6">
        <div class="recent-activity">
          <h5><i class="bi bi-briefcase me-2"></i>Recent Job Openings</h5>
          <% if (recentJobs && recentJobs.length > 0) { %>
            <% recentJobs.forEach(job => { %>
              <div class="activity-item">
                <div class="d-flex justify-content-between">
                  <div>
                    <strong class="job-title"><%= job.title %></strong>
                    <div class="d-flex align-items-center mt-1">
                      <span class="department-badge me-2"><%= job.department %></span>
                      <span class="status-badge <%= job.isActive ? 'active-badge' : 'inactive-badge' %>">
                        <%= job.isActive ? 'Active' : 'Inactive' %>
                      </span>
                    </div>
                  </div>
                  <div class="activity-date">
                    <%= new Date(job.createdAt).toLocaleDateString() %>
                  </div>
                </div>
              </div>
            <% }) %>
          <% } else { %>
            <div class="text-center py-3">No recent job openings</div>
          <% } %>
          <div class="text-end mt-3">
            <a href="/admin/dashboard?tab=jobs" class="btn btn-sm btn-outline-primary">
              View All Jobs <i class="bi bi-arrow-right"></i>
            </a>
          </div>
        </div>
      </div>
      <div class="col-lg-6">
        <div class="recent-activity">
          <h5><i class="bi bi-tags me-2"></i>Recently Added Brands</h5>
          <% if (recentBrands && recentBrands.length > 0) { %>
            <% recentBrands.forEach(brand => { %>
              <div class="activity-item">
                <div class="d-flex justify-content-between">
                  <div>
                    <strong class="brand-title"><%= brand.name %></strong>
                    <div class="d-flex align-items-center mt-1">
                      <span class="me-2"><%= brand.category %></span>
                      <span class="status-badge <%= brand.isActive ? 'active-badge' : 'inactive-badge' %>">
                        <%= brand.isActive ? 'Active' : 'Inactive' %>
                      </span>
                    </div>
                  </div>
                  <div class="activity-date">
                    <%= new Date(brand.createdAt).toLocaleDateString() %>
                  </div>
                </div>
              </div>
            <% }) %>
          <% } else { %>
            <div class="text-center py-3">No recent brands added</div>
          <% } %>
          <div class="text-end mt-3">
            <a href="/admin/dashboard?tab=brands" class="btn btn-sm btn-outline-primary">
              View All Brands <i class="bi bi-arrow-right"></i>
            </a>
          </div>
        </div>
      </div>
    </div>

    <!-- Tabbed Content -->
    <ul class="nav nav-tabs mb-4">
      <li class="nav-item">
        <a class="nav-link <%= (req.query.tab === 'jobs') ? 'active' : '' %>" 
           href="/admin/dashboard?tab=jobs">Jobs Management</a>
      </li>
      <li class="nav-item">
        <a class="nav-link <%= req.query.tab === 'brands' ? 'active' : '' %>" 
           href="/admin/dashboard?tab=brands">Brands Management</a>
      </li>
    </ul>

    <% if (req.query.tab === 'brands') { %>
      <!-- Brands Management Section -->
      <div class="page-header">
        <h3>Brands Management</h3>
        <a href="/admin/brand/add" class="btn btn-primary">
          <i class="bi bi-plus-lg"></i> Add New Brand
        </a>
      </div>

      <div class="card">
        <div class="card-body">
          <div class="table-responsive">
            <table class="table table-hover">
              <thead>
                <tr>
                  <th>Brand Name</th>
                  <th>Category</th>
                  <th>Card Type</th>
                  <th>Position</th>
                  <th>Status</th>
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody>
                <% if (brands && brands.length > 0) { %>
                  <% brands.forEach(brand => { %>
                    <tr>
                      <td class="brand-title"><%= brand.name %></td>
                      <td><%= brand.category %></td>
                      <td>
                        <span class="card-type-badge <%= brand.displayType === 'info-card' ? 'info-card-badge' : 'image-card-badge' %>">
                          <%= brand.displayType === 'info-card' ? 'Info Card' : 'Image Card' %>
                        </span>
                      </td>
                      <td><%= brand.position %></td>
                      <td>
                        <% if (brand.isActive) { %>
                          <span class="status-badge active-badge">Active</span>
                        <% } else { %>
                          <span class="status-badge inactive-badge">Inactive</span>
                        <% } %>
                      </td>
                      <td class="action-buttons">
                        <a href="/admin/brand/edit/<%= brand._id %>" class="btn btn-sm btn-outline-primary">
                          <i class="bi bi-pencil"></i> Edit
                        </a>
                        <button class="btn btn-sm btn-outline-danger" 
                                data-bs-toggle="modal" 
                                data-bs-target="#deleteBrandModal<%= brand._id %>">
                          <i class="bi bi-trash"></i> Delete
                        </button>
                      </td>
                    </tr>
                    
                    <!-- Delete Brand Modal -->
                    <div class="modal fade" id="deleteBrandModal<%= brand._id %>" tabindex="-1" aria-hidden="true">
                      <div class="modal-dialog">
                        <div class="modal-content">
                          <div class="modal-header">
                            <h5 class="modal-title">Confirm Delete</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                          </div>
                          <div class="modal-body">
                            Are you sure you want to delete the brand "<%= brand.name %>"?
                          </div>
                          <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                            <form action="/admin/brand/delete/<%= brand._id %>" method="POST">
                              <button type="submit" class="btn btn-danger">Delete</button>
                            </form>
                          </div>
                        </div>
                      </div>
                    </div>
                  <% }) %>
                <% } else { %>
                  <tr>
                    <td colspan="6" class="text-center">No brands available</td>
                  </tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    <% } else if (req.query.tab === 'jobs') { %>
      <!-- Jobs Management Section -->
      <div class="page-header">
        <h3>Job Listings</h3>
        <a href="/admin/job/add" class="btn btn-primary">
          <i class="bi bi-plus-lg"></i> Add New Job
        </a>
      </div>

      <div class="card">
        <div class="card-body">
          <div class="table-responsive">
            <table class="table table-hover">
              <thead>
                <tr>
                  <th>Job Title</th>
                  <th>Department</th>
                  <th>Type</th>
                  <th>Status</th>
                  <th>Created Date</th>
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody>
                <% if (jobs && jobs.length > 0) { %>
                  <% jobs.forEach(job => { %>
                    <tr>
                      <td class="job-title"><%= job.title %></td>
                      <td>
                        <span class="department-badge"><%= job.department %></span>
                      </td>
                      <td><%= job.jobType %></td>
                      <td>
                        <% if (job.isActive) { %>
                          <span class="status-badge active-badge">Active</span>
                        <% } else { %>
                          <span class="status-badge inactive-badge">Inactive</span>
                        <% } %>
                      </td>
                      <td><%= new Date(job.createdAt).toLocaleDateString() %></td>
                      <td class="action-buttons">
                        <a href="/admin/job/edit/<%= job._id %>" class="btn btn-sm btn-outline-primary">
                          <i class="bi bi-pencil"></i> Edit
                        </a>
                        <button class="btn btn-sm btn-outline-danger" 
                                data-bs-toggle="modal" 
                                data-bs-target="#deleteModal<%= job._id %>">
                          <i class="bi bi-trash"></i> Delete
                        </button>
                      </td>
                    </tr>
                    
                    <!-- Delete Modal -->
                    <div class="modal fade" id="deleteModal<%= job._id %>" tabindex="-1" aria-hidden="true">
                      <div class="modal-dialog">
                        <div class="modal-content">
                          <div class="modal-header">
                            <h5 class="modal-title">Confirm Delete</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                          </div>
                          <div class="modal-body">
                            Are you sure you want to delete the job "<%= job.title %>"?
                          </div>
                          <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                            <form action="/admin/job/delete/<%= job._id %>" method="POST">
                              <button type="submit" class="btn btn-danger">Delete</button>
                            </form>
                          </div>
                        </div>
                      </div>
                    </div>
                  <% }) %>
                <% } else { %>
                  <tr>
                    <td colspan="6" class="text-center">No job listings available</td>
                  </tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>